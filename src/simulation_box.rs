
pub struct SimulationBox {
    pub x: f64,
    pub y: f64,
    pub z: f64,
    pub alpha: f64,
    pub beta: f64,
    pub gamma: f64,
}

pub struct RectangularSimulationBox {
    pub x: f64,
    pub y: f64,
    pub z: f64,
}

pub struct SquareSimulationBox {
    pub x: f64,
}
